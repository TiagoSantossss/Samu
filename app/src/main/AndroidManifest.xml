<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.samu">

    <!-- Permissões necessárias -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/samu_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_samu_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Samu"
        tools:targetApi="31">

        <!-- Atividade principal da aplicação -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Intent Filter para receber o Redirect URI -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Substitua pelo esquema configurado no Uber Dashboard -->
                <data
                    android:scheme="com.seupacote.app"
                    android:host="redirect" />
            </intent-filter>
        </activity>

        <!-- Outras atividades -->
        <activity android:name=".Registo" android:exported="false" />
        <activity android:name=".MapsActivity" android:exported="false" />
    </application>
</manifest>
