<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.Samu" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cor da status bar -->
        <item name="android:statusBarColor">@color/laranja</item>
        <!-- Ícones escuros na status bar, se a cor de fundo for clara -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Samu" parent="Base.Theme.Samu" />

</resources>
